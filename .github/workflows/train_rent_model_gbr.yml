name: Train Rent Model (Gradient Boosting)

on:
  workflow_dispatch:   # تشغيل يدوي فقط -- تجربة Gradient Boosting كبديل لـ Random Forest

jobs:
  train:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: pip install pandas numpy scikit-learn joblib

      - name: Train rent model (GBR)
        run: python train_rent_model_gbr.py

      - name: Commit model files
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data/rent_model_gbr.joblib data/rent_model_gbr_district_encoding.joblib
          git commit -m "Train rent model GBR $(date +'%Y-%m-%d')" || echo "No changes"
          for i in 1 2 3 4 5; do
            git pull --rebase origin main && git push && break
            sleep 5
          done
